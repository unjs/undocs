import type { UndocsClientPlugin } from "../../src/app/plugins/types.ts";

/** Empty plugin list for unit tests (production uses the Vite virtual module). */
export const clientPlugins: UndocsClientPlugin[] = [];
